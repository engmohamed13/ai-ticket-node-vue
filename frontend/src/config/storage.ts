export const TOKEN_STORAGE_KEY = 'crm.auth.token';
export const USER_STORAGE_KEY = 'crm.auth.user';
/** Interface language. Survives sign-out on purpose — it is a device preference, not session state. */
export const LOCALE_STORAGE_KEY = 'crm.locale';
